<!-- {/* {
                todos.map((todo) => (
                    <li key={todo.id}>{todo.text}

                        <Button variant="outlined" color="error" onClick={() => dispatch(removeTodo(todo.id))}>
                            Delete
                        </Button>
                    </li>

                ))
            } */} -->todo.jsx